// n = numero de lados, s = soma dos ângulos internos

let n = 8;

// cálculo da soma dos ângulos internos
const s = (n - 2) * 180;
console.log(s);

// cálculo de quanto vale cada ângulo
const valorAngulo = s / n; 
console.log(valorAngulo);
